Basic typescript starter project for e2e testing using protractor or selenium-webdriver  

Run the following commands to setup
```
$> npm install
$> webdriver-manager update

```

Run 
```
$> tsc -w
$> webdriver-manager start
$> npm test


```